spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/item
    username: manager
    password: housemanager@ppdb

  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 100

#log
logging:
  level:
    com.house.item: info

#properties
properties:
  dir:
    photo: /home/pi/app/house/item/users/image/

#swagger
springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /api-docs